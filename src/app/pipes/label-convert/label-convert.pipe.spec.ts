import { LabelConvertPipe } from './label-convert.pipe';

describe('LabelConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
